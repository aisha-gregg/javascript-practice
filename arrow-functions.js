const greetings = function(name) {
  return "hello " + name;
};

const greetings = name => {
  return "hello " + name;
};

const greetings = name => {
  return "hello " + name;
};

const greetings = name => "hello " + name;

const greetings = (name, message) => "hello " + name + message;

const sorted = obj.sort((a, b) => (a > b ? -1 : 1));

const sorted = obj.sort(function(a, b) {
  return a > b ? -1 : 1;
});
